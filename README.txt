This program is a simulated linked list in Java.

The main goals for this program are:
User can input data.
The data functions as a linked list.
User can navigate through the list.
The data is transcribed to a text file.
Text files are read every program start.
Memory is cleaned after program termination, but text files remain.

The start date is March 15, 2017.